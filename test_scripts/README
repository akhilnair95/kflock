Some sample test scripts for kflock

Usage:
------------

Compile the lock.c program
cc -o lock lock.c

./lock <Key_Value> <SH or EX>
SH - shared lock
EX - exclusive lock

Setting the key value to -1 invokes the original flock system call.

key values permitted for kflock: 1 to sizeof(unsigned int)

Some sample test senarios are provided in shell scripts.
